const config = {
    route: {
        home: '/',
        following: '/following',
        profile: '/profile/:nickname',
        upload: '/upload',
        live: '/live',
    }
}

export default config;